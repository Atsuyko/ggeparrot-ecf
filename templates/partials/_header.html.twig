<nav class="navbar navbar-expand-lg bg-dark" data-bs-theme="dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="{{ path('home') }}">
      <img src="{{ asset('assets/img/GgeLogo.png') }}" alt="Logo" id="logo" height="25">
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01"
      aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarColor01">
      <ul class="navbar-nav me-auto">
        <li class="nav-item">
          <a class="nav-link shad" href="{{ path('home') }}">Accueil</a>
        </li>
        <li class="nav-item">
          <a class="nav-link shad" href="{{ path('service') }}">Espace entretien</a>
        </li>
        <li class="nav-item">
          <a class="nav-link shad" href="{{ path('car') }}">Espace occasion</a>
        </li>
      </ul>
      <ul class="navbar-nav ms-auto bg-dark mx-5">
        {% if app.user %}
        <li class="nav-item dropstart">
          <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true"
            aria-expanded="false">{{ app.user.firstname }} {{ app.user.lastname }}</a>
          <div class="dropdown-menu bg-dark">
            <a class="dropdown-item" href="{{ path('admin') }}">Système d'administration</a>
            <a class="dropdown-item" href="{{ path('car.new') }}">Créer une annonce</a>
            <a class="dropdown-item" href="{{ path('user.password', {id: app.user.id}) }}">Modifier mon mot de passe</a>
            <div class="dropdown-divider"></div>
            <a class="dropdown-item" href="{{ path('logout') }}">Déconnexion</a>
          </div>
        </li>
        {% else %}
        <li class="nav-item">
          <a class="nav-link" href="{{ path('login') }}">Connexion</a>
        </li>
        {% endif %}
      </ul>
    </div>
  </div>
</nav>