<nav class="navbar bg-dark footer" data-bs-theme="dark">
  <div class="container-fluid">
    <ul class="navbar-nav mx-auto">
      <li class="nav-item">
        <a class="nav-link shad" href="{{ path('contact') }}">Contact
        </a>
      </li>
      <li class="nav-item">
        <a class="nav-link shad" href="{{ path('notice') }}">Mentions Légales</a>
      </li>
      <li class="nav-item">
        <a class="nav-link shad" href="{{ path('policy') }}">Politique de confidentialité</a>
      </li>
    </ul>
    <ul class="navbar-nav mx-auto">
      <h5 class="text-white">Horaires d'ouverture</h3>
        {% for openingTime in openingTimes %}
        <li class="nav-item text-white">
          {{ openingTime.day }}
          {% if openingTime.openAM is not null and openingTime.closeAM is not null %}
          {{ openingTime.openAM|date("G:i") }} - {{ openingTime.closeAM|date("G:i") }} |
          {% else %} Fermé | {% endif %}
          {% if openingTime.openPM is not null and openingTime.closePM is not null %}
          {{ openingTime.openPM|date("G:i") }} - {{ openingTime.closePM|date("G:i") }}
          {% else %} Fermé {% endif %}
        </li>
        {% endfor %}
    </ul>
  </div>
</nav>
<nav class="navbar bg-dark footer" data-bs-theme="dark">
  <div class="container-fluid">
    <ul class="navbar-nav ms-auto me-auto">
      <li class="nav-item text-primary">
        Developed by Atsuyko
      </li>
    </ul>
  </div>
</nav>